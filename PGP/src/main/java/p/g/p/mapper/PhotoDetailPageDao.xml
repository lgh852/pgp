<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="p.g.p.dao.PhotoDetailPageDao">

<select id = "tetlist" resultType="p.g.p.model.PhotoDetailPageMaker">
select photo_name from board,board_photo where board.board_idx=board_photo.board_idx order by board_cnt desc limit #{pagenum},#{contentnum}
</select>

<select id="testcount" resultType="int">
select count(*) from board,board_photo where board.board_idx=board_photo.board_idx
</select>





</mapper>
